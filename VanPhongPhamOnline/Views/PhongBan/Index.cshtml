@model IEnumerable<VanPhongPhamOnline.Data.PhongBan>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenPb)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ThongTin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenPb)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ThongTin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaPb">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaPb">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaPb">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
